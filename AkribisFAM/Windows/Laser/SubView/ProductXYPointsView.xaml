<UserControl x:Class="AkribisFAM.Windows.ProductXYPointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AkribisFAM.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Border BorderThickness="1" CornerRadius="3" BorderBrush="Black">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="Gray" BorderThickness="0 0 0 2">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="Teachpoint "/>  
                    <Label Content="{Binding TeachPointIndex}" Grid.Column="0" />
                    <Button x:Name="btnMoveToPosLaserCheckAll" Grid.Column="1" Margin="10 0" Content="Move and check all" HorizontalAlignment="Right"
                            Click="btnMoveToPosLaserCheckAll_Click"/>
                </StackPanel>
            </Border>
            <ScrollViewer Grid.Row="1">
                <ItemsControl ItemsSource="{Binding}">
                    <ItemsControl.ItemsPanel>

                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:PointXYMoveView/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
