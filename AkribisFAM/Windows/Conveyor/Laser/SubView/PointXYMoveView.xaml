<UserControl x:Class="AkribisFAM.Windows.PointXYMoveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AkribisFAM.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Border BorderThickness="1" CornerRadius="3" BorderBrush="Black">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="X" Width="20" Grid.Column="0" FontWeight="Bold" FontSize="16" Foreground="DarkBlue" VerticalAlignment="Center"/>
            <Label Content="{Binding XPos}" Grid.Column="1" FontSize="16" Foreground="DarkBlue" VerticalAlignment="Center"/>
            <Label Content="Y" Width="20"  Grid.Column="2" FontWeight="Bold" FontSize="16" Foreground="DarkRed" VerticalAlignment="Center"/>
            <Label Content="{Binding YPos}" Grid.Column="3" Width="20" FontSize="16" Foreground="DarkRed" VerticalAlignment="Center"/>
            <Button x:Name="btnMoveToPos" Grid.Column="4" Margin="10 0" Content="Move"/>
            <Button x:Name="btnLaserCheck" Grid.Column="5" Margin="10 0" Content="Check"/>
            <Button x:Name="btnMoveToPosLaserCheck" Grid.Column="6" Margin="10 0" Content="Move + Check"/>
        </Grid>
      </Border>
</UserControl>
