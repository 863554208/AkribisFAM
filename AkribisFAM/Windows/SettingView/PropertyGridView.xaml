<UserControl x:Class="AkribisFAM.Windows.PropertyGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AkribisFAM"
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" DataContextChanged="UserControl_DataContextChanged">
    <UserControl.Resources>
        <Style TargetType="{x:Type ToggleButton}">
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <!--<Label Content="System setting"/>-->
        <toolkit:PropertyGrid x:Name="propGrid" Grid.Column="0" Grid.Row="0" SelectedObject="{Binding CacheParam}"  
                              ShowTitle="False"
                              KeyDown="propGrid_KeyDown"
                              FontSize="20" 
                              NameColumnWidth="400"  
                              PropertyValueChanged="PropertyGrid_PropertyValueChanged" Background="Transparent">

        </toolkit:PropertyGrid>
        <StackPanel x:Name="grpButtons" Grid.Column="0" Grid.Row="1" Orientation="Horizontal">
            <Button x:Name="btnSave" Click="ButtonSave_Click" Grid.Column="1" Margin="2" Width="150" IsEnabled="{Binding IsChanged}">Apply and Save</Button>
            <Button x:Name="btnLoad" Click="ButtonLoad_Click" Grid.Column="1" Margin="2" Width="100" IsEnabled="{Binding hasBackupFile}">Load</Button>
            <Button x:Name="btnBackToOri" Click="ButtonUnchange_Click" Grid.Column="1" Margin="2" Width="100" IsEnabled="{Binding IsChanged}">Undo</Button>
        </StackPanel>
    </Grid>
</UserControl>
