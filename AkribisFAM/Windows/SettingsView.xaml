<UserControl x:Class="AkribisFAM.Windows.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:con="clr-namespace:AkribisFAM.Windows.Converters"
             xmlns:window="clr-namespace:AkribisFAM.Windows"
             xmlns:tk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <con:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>

    <Grid>
        <TabControl>
            <TabItem Header="System"  x:Name="tabSystem">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>

                    <window:PropertyGridView x:Name="pgridSystem" DataContext="{Binding Lprm}" IsEnabled="{Binding Machine.IsRunning , Converter= {StaticResource InverseBooleanConverter}}" Grid.Row="1" Grid.RowSpan="2"></window:PropertyGridView>
                </Grid>
            </TabItem>
            <TabItem Header="Recipe" x:Name="tabRecipe">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal">
                        <Label Content="RECIPE" FontSize="30" VerticalAlignment="Center"/>
                        <Label Content=":" VerticalAlignment="Center"/>
                        <ComboBox Width="300" 
                                  HorizontalAlignment="Left" 
                                  Padding="5"
                                  FontSize="16"
                                  FontWeight="Bold"
                                  ItemsSource="{Binding RecipeMngr.Recipes}"
                                  SelectedIndex="0"
                                  SelectionChanged="ComboBox_SelectionChanged"
                                  MaxDropDownHeight="Auto"
                                  Style="{StaticResource MaterialDesignFilledComboBox}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card Background="White" BorderThickness="5" materialDesign:ElevationAssist.Elevation="Dp4" >
                                        <StackPanel Orientation="Vertical" >
                                        <TextBlock Text="{Binding RecipeName}" HorizontalAlignment="Center"/>
                                        <Image Source="{Binding ImageFilePath}" Width="100" Height="100" Margin="2"/>
                                        </StackPanel>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>


                        </ComboBox>
                    </StackPanel>

                    <window:PropertyGridView x:Name="pgridRecipe" DataContext="{Binding RecipePrm}" IsEnabled="{Binding Machine.IsRunning , Converter= { StaticResource InverseBooleanConverter}}" Grid.Row="1" Grid.RowSpan="2"></window:PropertyGridView>
                </Grid>
            </TabItem>
        </TabControl>
 
    </Grid>
</UserControl>
